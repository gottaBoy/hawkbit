# HorizontalPodAutoscaler for zota-server (DDI + MGMT).
# Scales based on CPU (70%) + memory (80%).
# Java process uses ~650MB steady-state; request=1Gi → baseline ~63%.
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: zota-server
  namespace: zota
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: zota-server
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 50
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
        - type: Percent
          value: 100
          periodSeconds: 30
