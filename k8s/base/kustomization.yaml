apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: zota

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - pvc.yaml

images:
  - name: zota-server
    newTag: latest
